Your goal is to update any valnerable dependencies.

Do the following:

1. Run `npm audit` to find valnerable installed packagee in this project
2. Run `npm audit fix` to apply update
3. Run tests and verify the updates did't break anything